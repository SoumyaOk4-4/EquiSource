<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>EquiSource : Best place to rent</title>
  <script src="/main.js"></script>
</head>
<body>
  <h1>EquiSource</h1>
  <div>
    <% items.forEach(item => { %>
      <div>
        <h2><%= item.name %></h2>
        <img src="/uploads/<%= item.image %>" alt="<%= item.name %>" style="width:200px;height:auto;">
        <p>Price: Rs.<%= item.price %></p>
        <p>Status: <%= item.available ? 'Available' : 'Not Available' %></p>
        <button onclick="rentItem('<%= item.name %>')">Rent</button>
      </div>
    <% }) %>
  </div>
</body>
</html>
